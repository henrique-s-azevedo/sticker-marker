[build]
buildCommand = "cd backend && ./mvnw package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/sticker-marker-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 60
